"use client";
import { useRef, useState, type FormEvent } from "react"; import type { Trip } from "@/app/lib/trips"; import { createTransportId, saveTransport, TRANSPORT_MODES, TRANSPORT_MODE_LABELS, validateTransportValues, type TransportMode, type TransportValues } from "@/app/lib/transports"; import { inputClass } from "@/app/components/ui";
function initial(trip: Trip) { const n = new Date(), p = (x:number) => String(x).padStart(2,"0"), today = `${n.getFullYear()}-${p(n.getMonth()+1)}-${p(n.getDate())}`; return { date: today >= trip.startDate && today <= trip.endDate ? today : trip.startDate, time: `${p(n.getHours())}:${p(n.getMinutes())}` }; }
export default function QuickTransportForm({trip,onClose}:{trip:Trip;onClose:()=>void}) { const [start] = useState(() => initial(trip)); const [mode,setMode] = useState<TransportMode>("shinkansen"); const [errors,setErrors] = useState<ReturnType<typeof validateTransportValues> & {storage?:string}>({}); const [saving,setSaving] = useState(false); const lock=useRef(false);
  function submit(e:FormEvent<HTMLFormElement>) { e.preventDefault(); if(lock.current)return; const d=new FormData(e.currentTarget), t=(n:string)=>String(d.get(n)??"").trim(); const v:TransportValues={mode,departurePlace:t("departurePlace"),arrivalPlace:t("arrivalPlace"),departureDate:t("departureDate"),departureTime:t("departureTime"),arrivalDate:"",arrivalTime:t("arrivalTime"),serviceName:t("serviceName"),operator:"",departurePlatform:"",arrivalPlatform:"",terminal:"",carNumber:t("carNumber"),seatNumber:t("seatNumber"),fare:"",bookingUrl:"",memo:"",reservationId:""}; const found=validateTransportValues(v,trip.startDate,trip.endDate); if(Object.keys(found).length){setErrors(found);return;} lock.current=true;setSaving(true);const now=new Date().toISOString();try{saveTransport({...v,id:createTransportId(),tripId:trip.id,arrivalDate:v.arrivalTime?v.departureDate:"",fare:null,createdAt:now,updatedAt:now});onClose();}catch{lock.current=false;setSaving(false);setErrors({storage:"移動を保存できませんでした。入力内容は維持されています。"});}}
  return <form onSubmit={submit} noValidate className="mt-4 rounded-2xl bg-white p-4 sm:p-5"><h3 className="font-bold">移動のクイック追加</h3><p className="mt-1 text-sm text-stone-500">出発地から到着地への移動をすばやく登録します。</p>{errors.storage&&<p role="alert" className="mt-3 rounded-xl bg-red-50 p-3 text-sm text-red-700">{errors.storage}</p>}<div className="mt-4 grid gap-4 sm:grid-cols-2"><Field label="移動手段" error={errors.mode}><select value={mode} onChange={(e)=>setMode(e.target.value as TransportMode)} className={inputClass}>{TRANSPORT_MODES.map(x=><option key={x} value={x}>{TRANSPORT_MODE_LABELS[x]}</option>)}</select></Field><div className="hidden sm:block"/><Field label="出発地" error={errors.departurePlace}><input name="departurePlace" className={inputClass}/></Field><Field label="到着地" error={errors.arrivalPlace}><input name="arrivalPlace" className={inputClass}/></Field><Field label="出発日" error={errors.departureDate}><input name="departureDate" type="date" min={trip.startDate} max={trip.endDate} defaultValue={start.date} className={inputClass}/></Field><Field label="出発時刻" error={errors.departureTime}><input name="departureTime" type="time" defaultValue={start.time} className={inputClass}/></Field><Field label="到着時刻" optional error={errors.arrivalTime}><input name="arrivalTime" type="time" className={inputClass}/></Field><Field label="列車名・便名など" optional><input name="serviceName" className={inputClass}/></Field>{["shinkansen","limited-express","highway-bus","airplane","ferry"].includes(mode)&&<div className="contents"><Field label="号車" optional><input name="carNumber" className={inputClass}/></Field><Field label="座席" optional><input name="seatNumber" className={inputClass}/></Field></div>}</div><p className="mt-4"><a href={`/trips/${trip.id}/transports/new`} className="inline-flex min-h-12 items-center text-sm font-bold text-teal-800">詳細入力へ</a></p><div className="mt-3 flex gap-3 sm:justify-end"><button type="button" onClick={onClose} className="min-h-12 flex-1 rounded-xl bg-stone-100 px-4 font-bold sm:flex-none">キャンセル</button><button disabled={saving} aria-busy={saving} className="min-h-12 flex-[2] rounded-xl bg-teal-700 px-5 font-bold text-white disabled:opacity-60 sm:flex-none">{saving?"保存中…":"保存する"}</button></div></form>; }
function Field({label,optional,error,children}:{label:string;optional?:boolean;error?:string;children:React.ReactNode}){return <label className="text-sm font-bold">{label}<span className={`ml-2 text-xs font-normal ${optional?"text-stone-400":"text-red-700"}`}>{optional?"任意":"必須"}</span>{children}{error&&<span role="alert" className="mt-1 block font-normal text-red-700">{error}</span>}</label>}
